<nav class="navbar navbar-expand-lg bg-dark text-white navbar-dark">
  <div class="container-fluid">

    <%= link_to "FitBuddy", root_path, class: "navbar-brand h1 text-white" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon text-white"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if logged_in? %>
          <% if current_user.admin %>
          <li class="nav-item nav-link active">
            <%= link_to "(Admin)"+current_user.username, current_user, class: "nav-link active" %>
          </li>
          <% else %>
          <li class="nav-item nav-link active">
            <%= link_to "(Profile)"+current_user.username, current_user, class: "nav-link active" %>
          </li>
          <% end%>


        <% else %>
          <li class="nav-item">

            <%= link_to "About", about_path, class:"nav-link active" %>
          </li>
        <% end %>


        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to "Logout", logout_path, class:"nav-link active"%>
          </li>
        <% else %>
        <li class="nav-item">
          <%= link_to "Log in", login_path, class:"nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign up", signup_path, class:"nav-link active" %>
        </li>
        <% end %>
        <% if logged_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Groups
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "Create a new Group", newgroup_path, class:"dropdown-item" %>
            <%= link_to "Find a Group", groups_path, class:"dropdown-item" %>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
        <% else %>
        <% end %>
        <li class="nav-item">
          <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
        </li>
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
